{
  "name": "pasero-eln-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/assets && cp -R *.html *.css *.js assests dist/ && cp -R assets/pasero-story dist/assets/"
  },
  "displayName": "Pasero"
}
